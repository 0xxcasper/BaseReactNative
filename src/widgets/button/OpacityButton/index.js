import React, { memo } from 'react'
import PropTypes from 'prop-types'
import { View } from 'react-native'

const OpacityButton = memo((props) => {
    return (
        <View/>
    )
})

OpacityButton.propTypes = {

}

export default OpacityButton
